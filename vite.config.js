import { defineConfig } from 'vite';

export default defineConfig({
  // WAŻNE: Zmień 'nazwa-twojego-repozytorium' na dokładną nazwę repozytorium,
  // którą utworzysz za chwilę na GitHubie (np. 'teb-merge-game').
  // Pamiętaj o ukośnikach na początku i na końcu!
  base: '/teb-merge-game/', 
});